/**
 * LoginPage was removed during the Clerk migration.
 * Auth UI is now handled by AuthPage — see __tests__/pages/AuthPage.test.jsx
 *
 * This file is retained as a placeholder so the test suite stays importable.
 */

describe("LoginPage (retired)", () => {
  it("placeholder — see AuthPage.test.jsx for auth page coverage", () => {
    expect(true).toBe(true);
  });
});
