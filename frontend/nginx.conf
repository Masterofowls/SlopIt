server {
  listen 8080;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  location /assets/ {
    expires 1y;
    add_header Cache-Control "public, immutable";
  }

  location /api/v1/ {
    proxy_pass https://slopit-api.fly.dev;
    proxy_set_header Host slopit-api.fly.dev;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    proxy_http_version 1.1;
    proxy_ssl_server_name on;
    proxy_redirect off;
  }

  location /accounts/ {
    proxy_pass https://slopit-api.fly.dev;
    proxy_set_header Host slopit-api.fly.dev;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    proxy_http_version 1.1;
    proxy_ssl_server_name on;
    # Rewrite backend-origin redirects (e.g. post-login) back to the frontend
    # so the browser stays on peaceful-flower-536.fly.dev after OAuth completes.
    proxy_redirect https://slopit-api.fly.dev/ https://peaceful-flower-536.fly.dev/;
  }
}
